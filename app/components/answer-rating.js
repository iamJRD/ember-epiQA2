import Ember from 'ember';

export default Ember.Component.extend({
  // upVote: this.get(answer.upVote),
  // downVote: this.get(answer.downVote),

  // actions: {
  //   upVote(upVote) {
  //     upVote: this.get(answer.upVote);
  //   }
  // }
});
