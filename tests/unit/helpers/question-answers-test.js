import { questionAnswers } from 'epi-qa/helpers/question-answers';
import { module, test } from 'qunit';

module('Unit | Helper | question answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionAnswers([42]);
  assert.ok(result);
});
